# My entire config

## clone
first clone nixos-config into /etc/nixos and run nixos-rebuild switch
then run this.

```shell
git clone git@github.com:aster-void/dotfiles.git ~/.dotconfig && ~/.config/init.sh
```

## available apps

kitty, alacritty, hyprland, waybar, and all apps within home-manager and nixos-config.
