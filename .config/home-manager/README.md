# nixos-home


## reproduce my config
to reproduce my config, you also need to clone nixos-config (/etc/nixos), hypr-config (~/.config/hypr) and alacritty-config (~/.config/alacritty)
there are probably still some things that I'm missing, and I'm planning to integrate those as well as those repos above.
