# pre-push:
#   commands:
#     packages-audit:
#       tags: frontend security
#       run: yarn audit
#     gems-audit:
#       tags: backend security
#       run: bundle audit

pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.nix"
      run: alejandra {staged_files}
    deadnix:
      glob: "*.nix"
      run: deadnix .
