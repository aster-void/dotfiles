$mainMonitor = DP-1
$subMonitor = DP-2
$resolution = highrr # 1920x1080@60

monitor=$mainMonitor,$resolution,0x0,1
# monitor=$subMonitor,$resolution,0x0,$scale,mirror,$mainMonitor

# mouse sens is dependent on hardware
input {
	sensitivity = 0
}

# plugins
# source=./plugins/no-blur.hyprland.conf
# source=./plugins/no-animations.hyprland.conf
# source=./plugins/full-opacity.hyprland.conf
# source=./plugins/save-battery.hyprland.conf
